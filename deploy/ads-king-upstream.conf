# ads-king CDN upstream (deuwy). Some public DNS A records are dead (.121);
# pin working edges and fail over.
upstream ads_king_deuwy {
    server 103.96.73.194:443 max_fails=2 fail_timeout=20s;
    server 103.96.72.132:443 max_fails=2 fail_timeout=20s;
    keepalive 16;
}
